using UnityEngine;

/// <summary>
/// A marker component for road objects on the grid.
/// </summary>
public class Road : MonoBehaviour
{
    // Currently just a marker, but can hold road-specific data later (e.g. movement cost)
}
